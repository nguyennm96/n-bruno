version: '3.8'

services:
  mongodb:
    image: mongo:7
    container_name: bruno-server-mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    environment:
      MONGO_INITDB_DATABASE: bruno_server
    restart: unless-stopped

volumes:
  mongo-data:
